function Invalid(){
    return (
        <div className="invalid">
            Invalid State. Red(z) - Green(x) must equal Yellow(y). x, y, and z must be greater than zero. 
        </div>
    )
}

export default Invalid;